{% extends 'base.html.twig' %}

{% block title %}Edit Produit
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edition d'un produit</h1>

        {{ include('produit/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('produit_index') }}" class="btn btn-secondary mt-2 mb-2">retour à la liste</a>

        {{ include('produit/_delete_form.html.twig') }}
    </div>
{% endblock %}
